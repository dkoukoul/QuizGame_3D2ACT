<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.3D2ACTQuiz" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="fontFamily">sans-serif-medium</item>
        <item name="android:textColor">@color/dark_grey</item>
        <!-- Customize your theme here. -->
        <item name="android:colorPrimaryDark">@color/dark_green</item>
        <item name="android:colorPrimary">@color/white</item>
    </style>
    <style name="ButtonStyle" parent="Theme.3D2ACTQuiz">
        <item name="backgroundTint">@color/buttons</item>
        <item name="android:textColor">@color/button_text</item>
    </style>
</resources>